/* exported getLastChar */
function getLastChar(string) {
  return string[string.length - 1];
}

// define function with one paramter
// return value of string at index which is the length of string minus 1
